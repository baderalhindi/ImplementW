<Project Sdk="Microsoft.NET.Sdk.Web">
  <!-- L-4: references Application; Infrastructure only from the composition root (Program.cs). -->
  <ItemGroup>
    <ProjectReference Include="../PMPlatform.Application/PMPlatform.Application.csproj" />
    <ProjectReference Include="../PMPlatform.Infrastructure/PMPlatform.Infrastructure.csproj" />
  </ItemGroup>
  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.OpenApi" />
    <!-- TASK-028: validates the platform's own session tokens on every request (api-conventions R-46). -->
    <PackageReference Include="Microsoft.AspNetCore.Authentication.JwtBearer" />
  </ItemGroup>
  <ItemGroup>
    <InternalsVisibleTo Include="PMPlatform.Tests.Integration" />
  </ItemGroup>
  <ItemGroup>
    <!-- TASK-013: the template is documentation and a developer's Local.json is a secret; neither ships. -->
    <Content Update="appsettings.Template.json" CopyToPublishDirectory="Never" />
    <Content Update="appsettings.*.Local.json" CopyToPublishDirectory="Never" />
  </ItemGroup>
</Project>
